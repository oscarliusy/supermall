// actionTypes的格式与react相统一
// 当出现重名key时，会报错，避免重名函数

export default {
  ADD_COUNTER:'addCount',
  ADD_TO_CART:'addToCart',
  TOGGLE_CHECKED:'toggleChecked',
  ALL_CHECKED:'allChecked',
  ALL_UNCHECKED:'allUnchecked'
}
